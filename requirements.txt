# Core
chromadb
# Postgres + pgvector (replaces chromadb at runtime)
psycopg[binary]>=3.1
psycopg-pool>=3.2
pgvector>=0.3
sentence-transformers
flask
requests
numpy

# ZIM extraction
libzim
beautifulsoup4
langchain-text-splitters
pypdf

# OCR (optional — only needed if using --ocr flag with extract_zim.py)
# Install all three for tesseract engine:
#   pip install pymupdf opencv-python-headless pillow pytesseract
#   macOS:  brew install tesseract
#   Linux:  sudo apt install tesseract-ocr
#
# Or for the pure-Python easyocr engine (no binary needed):
#   pip install pymupdf opencv-python-headless easyocr
